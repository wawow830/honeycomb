---
name: research
description: "Gather external findings and sources for validation."
disable-model-invocation: true
---

`Read` `input`.
`Read` its references.
`Research` external information needed to validate idea.
`Assess` sources for credibility and relevance.
`Record` findings and sources for downstream use.
`Write` `=/validate/research.yaml` using only `research` from `^/../TEMPLATE.yaml`.
